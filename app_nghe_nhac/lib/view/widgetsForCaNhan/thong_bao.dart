import 'package:flutter/material.dart';

class TenManHinh extends StatelessWidget {
  const TenManHinh({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Giới thiệu ứng dụng"),
      ),
      body: const Center(child: Text("không có thông báo nào")),
    );
  }
}
